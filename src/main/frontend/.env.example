# Optional: override API base for the generated OpenAPI client and axios
# By default, the app uses relative '/api' which works with Vite proxy in dev
# and with Spring Boot reverse proxy in production.
# VITE_API_BASE=http://localhost:8080/api
